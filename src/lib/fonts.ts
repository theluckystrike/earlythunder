/**
 * System font stack — no Google Fonts.
 * Font families are defined in globals.css via @theme inline.
 * This module exists to keep layout.tsx imports stable.
 */
export const fontVariables = "";
